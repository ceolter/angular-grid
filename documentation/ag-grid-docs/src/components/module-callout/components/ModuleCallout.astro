---
import { ModuleCallout } from '@components/module-callout/components/ModuleCallout';

interface Props {
    moduleName: string;
}

const { moduleName } = Astro.props as Props;
---

<ModuleCallout moduleName={moduleName} client:only="react" />
